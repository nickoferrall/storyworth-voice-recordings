query GetScoreSettingById($id: String!) {
  getScoreSettingById(id: $id) {
    id
    competitionId
    penalizeIncomplete
    penalizeScaled
    scoring
    handleTie
    createdAt
    updatedAt
  }
}
