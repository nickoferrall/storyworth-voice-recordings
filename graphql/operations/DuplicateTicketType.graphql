mutation DuplicateTicketType($originalId: String!) {
  duplicateTicketType(originalId: $originalId) {
    id
    name
    description
    maxEntries
    price
    teamSize
    currency
    isVolunteer
    competitionId
    allowHeatSelection
    registered
    createdAt
    updatedAt
  }
}
