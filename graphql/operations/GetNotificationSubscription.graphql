query GetNotificationSubscription($email: String!) {
  getNotificationSubscription(email: $email) {
    id
    email
    eventType
    countries
    locations
    tags
    gender
    difficulty
    teamSize
  }
}
