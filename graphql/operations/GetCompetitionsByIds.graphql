query GetCompetitionsByIds($ids: [String!]!) {
  getCompetitionsByIds(ids: $ids) {
    id
    name
    registrationsCount
    participantsCount
    teamsCount
    registrationTrend {
      date
      count
      cumulativeCount
    }
  }
}
