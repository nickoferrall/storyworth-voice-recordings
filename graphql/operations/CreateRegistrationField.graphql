mutation CreateRegistrationField(
  $ticketTypeIds: [String!]!
  $registrationField: RegistrationFieldInput!
) {
  createRegistrationField(
    ticketTypeIds: $ticketTypeIds
    registrationField: $registrationField
  ) {
    id
    question
    type
    requiredStatus
    options
  }
}
