mutation LinkDirectoryCompToCompetition(
  $directoryCompId: String!
  $competitionId: String!
) {
  linkDirectoryCompToCompetition(
    directoryCompId: $directoryCompId
    competitionId: $competitionId
  )
}
